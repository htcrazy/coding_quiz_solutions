from collections import namedtuple

num_students = int(input())
col_names = ",".join(input().split())
Grades = namedtuple("Grades", col_names)

total = 0
for i in range(num_students):
    a, b, c, d = input().split()
    total += int(Grades(a, b, c, d).MARKS)

print(total / num_students)
