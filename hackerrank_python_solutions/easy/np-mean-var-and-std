import numpy

n, m = map(int, input().split())

array = [list(map(int, input().split())) for i in range(n)]

nump_arr = numpy.array(array)

print(numpy.mean(nump_arr, axis = 1))
print(numpy.var(nump_arr, axis = 0))
print(round(numpy.std(nump_arr), 11))
