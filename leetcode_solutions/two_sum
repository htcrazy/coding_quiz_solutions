class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:

        for i in range(len(nums)):
            subtracted = target - nums[i]

            if subtracted in nums[i+1:]:
                return [i, nums[i+1:].index(subtracted)+i+1]
