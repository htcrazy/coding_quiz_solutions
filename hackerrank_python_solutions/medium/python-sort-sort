n, m = map(int, input().split())

arr = [list(map(int, input().split())) for _ in range(n)]

k = int(input())

for thing in sorted(arr, key= lambda x: x[k]):
    print(" ".join(str(x) for x in thing))
