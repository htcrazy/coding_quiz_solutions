class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        width = len(needle)
        ans, i = -1, 0

        while ans < 0 and i <= len(haystack) - width:
            if haystack[i : i+width] == needle:
                ans = i
                break
            i += 1

        return ans
