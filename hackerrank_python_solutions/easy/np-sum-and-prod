import numpy

n, m = map(int, input().split())

array = [list(map(int, input().split())) for i in range(n)]
    
nump_array = numpy.array(array)
nump_sum = numpy.sum(nump_array, axis = 0)
print(numpy.prod(nump_sum))
